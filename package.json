{
  "name": "user-manager",
  "version": "1.0.0",
  "license": "MIT",
  "author": "Ryan Izac <io.ryan@outlook.com>",
  "description": "A Standalone Service or Integrated Library to manage users.",
  "keywords": [
    "users",
    "manager",
    "service"
  ],
  "workspaces": [
    "packages/*"
  ],
  "lint-staged": {
    "*.ts": [
      "prettier --ignore-path .prettierignore.precommit --write",
      "jest --bail --findRelatedTests --passWithNoTests"
    ]
  },
  "scripts": {
    "prepare": "husky install",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.1",
    "@types/node": "^18.16.1",
    "husky": "^8.0.3",
    "jest": "^29.5.0",
    "lint-staged": "^13.2.2",
    "prettier": "^2.8.8",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.4"
  }
}
